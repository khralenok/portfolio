<section id="hero">
  <div id="illustration">
    <img src="img/hero-illustration.svg" />
  </div>
  <div class="hero-text">
    <div id="title">
      <h1>
        <span id="h1-1">Looking for</span>
        <span id="h1-2">a graphic designer?</span>
      </h1>
    </div>
    <div id="subtitle">
      You found one. Hi, I’m Grigorii Khralenok. I design brand comunications
      and build systems and automations that make the work behind it run
      smoothly.
    </div>
    <div id="cta">
      <a class="btn" href="/">Watch showreel</a><a class="btn" href="#cases"
        >Explore cases</a
      >
    </div>
  </div>
</section>

<style>
  #hero {
    width: 100%;
    color: white;
    font-family: var(--font-inter);
    z-index: 1;
    font-size: 1.25vw;
    line-height: 1.5;
  }

  .hero-text {
    display: flex;
    flex-direction: column;
    padding: 4rem 25vw;
    background-color: var(--color-accent);
  }

  #illustration {
    position: relative;
    aspect-ratio: 320/66;
    overflow: visible;
    z-index: 10;
  }

  img {
    position: absolute;
    inset: 0;
    width: 100%;
  }

  #title,
  #subtitle,
  #cta {
    display: flex;
    align-items: center;
    text-align: center;
  }

  #title {
    grid-column: span 2;
    display: flex;
    justify-content: center;
  }

  h1 {
    margin: 0;
    color: inherit;
    line-height: 1;
    font-family: var(--font-fredoka);
    font-weight: 700;
    text-transform: uppercase;
  }

  h1 span:nth-child(1) {
    display: block;
    font-size: 7.3vw;
    font-stretch: 103%;
    white-space: nowrap;
  }

  h1 span:nth-child(2) {
    display: block;
    font-size: 4.33vw;
    font-stretch: 105%;
    white-space: nowrap;
  }

  #subtitle {
    margin-block: 3rem;
    letter-spacing: 0.07rem;
  }

  #cta {
    gap: 1rem;
    margin-block-end: 1rem;
  }

  .btn {
    display: flex;
    justify-content: center;
    flex-grow: 1;
    font-weight: 600;
    padding: 1rem 2rem;
    border-radius: 2rem;
    font-size: 1.25vw;
    text-decoration: none;
    color: var(--color-accent);
    background: white;
    box-shadow: var(--color-neutral-100) 0 1rem;
    border: 2px solid var(--color-neutral-100);
  }
</style>
