---
import CaseCard from "../components/CaseCard.astro";
import cryptonixCover from "../images/cryptonix.jpg"
import ccCover from "../images/cc.jpg"
import zvukCover from "../images/zvuk.jpg"

const cases = [
  {
    title: "Cryptonix",
    niche: "FinTech, B2B",
    scope: "Branding, Design System, Visuals",
    image: cryptonixCover.src,
    href: "/cryptonix",
  },
  {
    title: "Crowd Control",
    niche: "MarTech, B2B",
    scope: "Branding, Design System, Mascot",
    image: ccCover.src,
    href: "/crowd-control",
  },
  {
    title: "Zvuk Business",
    niche: "MarTech, B2B",
    scope: "Branding, Design System, Illustrations",
    image: zvukCover.src,
    href: "/zvuk",
  },
];

const actualCases = [];

for (const item of cases) {
  if (item.href != Astro.url.pathname) {
    actualCases.push(item);
  }
}
---

<section id="cases">
  {
    actualCases.map((item) => (
      <CaseCard
        title={item.title}
        niche={item.niche}
        scope={item.scope}
        image={item.image}
        href={item.href}
      />
    ))
  }
</section>

<style>
  section {
    display: flex;
  }

  @media (max-width: 1024px) {
    section {
      flex-direction: column;
    }
  }
</style>
